[project]
name = "2048-idle-evolution"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pygame-ce",
    "pyyaml",
]

[tool.uv.sources]
pygame-core = { path = "src/pygame_core", editable = true }
